[project]
name = "qpsk-dm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "April", email = "18055219130@163.com" }
]
requires-python = ">=3.12"
dependencies = [
    "numpy>=1.26",
    "scipy>=1.12",
    "matplotlib>=3.8",
    "pyyaml>=6.0",
    "pillow>=12.2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["demodulator"]
